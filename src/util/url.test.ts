// import { getImageUrlId } from './url'

// describe('getImageUrlId', () => {
// 	it('should return the image ID when given a URL with a PNG name', () => {
// 		const url = 'https://example.com/images/image.png'
// 		const result = getImageUrlId(url)
// 		expect(result).toBe('image.png')
// 	})

// 	it('should return the full URL when given a URL without a PNG name', () => {
// 		const url = 'https://example.com/images/image.jpg'
// 		const result = getImageUrlId(url)
// 		expect(result).toBe(url)
// 	})

// 	it('should return the full URL when given a URL without any slashes', () => {
// 		const url = 'https://example.com'
// 		const result = getImageUrlId(url)
// 		expect(result).toBe(url)
// 	})
// })
